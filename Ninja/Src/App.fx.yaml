App As appinfo:
    BackEnabled: =false
    OnStart: |+
        =Set(varItem,1);Set(varRows,CountRows(Banner));Set(varStart,false);
        
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

